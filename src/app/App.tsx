import { PRODUCT } from "@shared/branding";

export default function App() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-[#FFFDF7] font-body">
      <h1 className="font-display text-3xl text-[#2F6F5E]">{PRODUCT}</h1>
    </main>
  );
}
